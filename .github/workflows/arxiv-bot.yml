name: 'Arxiv Paper Bot'

on:
  schedule:
    # 每天 UTC 24:00 运行（北京时间早上 8 点）
    - cron: '0 14 * * *'
  workflow_dispatch:   # 手动触发

jobs:
  arxiv-paper-bot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install feedparser requests pyyaml python-dotenv zai-sdk

      - name: Run Arxiv Fetch
        run: |
          python paperPostman.py -c cs.CL cs.AI

      - name: Convert results to Text
        run: |
          YESTERDAY=$(TZ='Asia/Shanghai' date -d 'yesterday' +'%Y%m%d')
          echo "YESTERDAY=$YESTERDAY" >> $GITHUB_OUTPUT
          echo "Arxiv Papers for ${YESTERDAY}" > email.txt
          echo "============================================" >> email.txt
          FILES=$(ls cs.*_papers_${YESTERDAY}.txt 2>/dev/null || true)
          if [ -z "$FILES" ]; then
            echo "No result files found." >> email.txt
          else
            for file in $FILES; do
              if [ -s "$file" ]; then
                CATEGORY=$(echo $file | cut -d'_' -f1)
                echo -e "\nCategory: ${CATEGORY}" >> email.txt
                echo "--------------------------------------------" >> email.txt
                cat $file >> email.txt
                echo -e "\n" >> email.txt
              fi
            done
          fi

      - name: Set email subject
        id: subject
        run: |
          YESTERDAY=$(TZ='Asia/Shanghai' date -d 'yesterday' +'%Y-%m-%d')
          echo "subject=Arxiv Papers for ${YESTERDAY}" >> $GITHUB_OUTPUT

      - name: Send mail
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.qq.com
          server_port: 465
          secure: true
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: ${{ steps.subject.outputs.subject }}
          body: file://email.txt
          to: ${{ secrets.RECEIVER }}
          from: "Ignorance <${{ secrets.MAIL_USERNAME }}>"
          content_type: text/plain

      - name: Clean old files
        run: rm -f cs.*_papers_*.txt
